How far did the agent get? Did it fully implement your spec? What percentage of your acceptance criteria passed on the first try?

I was honestly really impressed with the agent. It got very far and implemented the entire quiz without large errors/problems. I tested the quiz after running the agent and everything it created matched my vision. I also tested many edge cases that might crash the app/cause undesired behavior (i.e. trying to log in with no account created, entering invalid characters, going to performance statistics when I haven't done a quiz yet, etc) and they were all handled perfectly. I would say that all of my acceptance crieria passed on the first try. 

Where did you intervene? List each time you had to step in during Phase 2. Why was the intervention needed? Could a better spec have prevented it?

I did not have to intervene at all during phase 2. I gave the Claude Code a detailed prompt (listed below) and had it work until it finished. When it finished the only thing I had to do was add in my own key for the API function. I predict that if I did not specify in the spec how to handle the API without my eye, I might have had to intervene at that point. However, in the prompt I told Claude how to handle the APIs which made the API integration super smooth. I think the specific spec helped a lot with the fact that I didn't need any intervention. I spent some extra time making sure the spec handled my exact vision and what I wanted to see out of the app. The most helpful part was probably the user workflow portion that showed step by step what running the app would be like. 

Claude Code prompt:
Read SPEC.md in this project. Implement the full quiz application exactly as specified. Read through the workflow and understand how the app will run. Create all necessary files. Include the questions from the file question.json. Make sure error handling matches what the spec describes. After creating the files, run the app to verify it starts without errors. For the API setup the API with placeholders for the key. I will input my own key later. Setup the template throughout the code and in .env. Make sure it works perfectly when I have to put my own key in at the end.
Read this documentation for openrouter: https://openrouter.ai/docs/quickstart

How useful was the AI review? Did it catch real bugs? Did it miss anything important? Did it flag things that weren't actually problems?

The AI review was very useful even though the app ran smoothly on the first try. The AI review was good at catching edge cases that I might was not able to fully test/did not have the time to test. Some of the bugs it caught was that score would display -5 even when we were already at a score of 0. There was also the bug of the minimum character length for login information not being enforced. I think some of the things it flagged weren't real problems such as the security issues. However, it flagged them as warn which I believe is warrented so the AI review did a good job. 

Spec quality → output quality: In hindsight, what would you change about your spec to get a better result from the agent?

If I wanted to get a better result from the agent I think in the spec I would have told it to rigorously test the app. The AI review caught some very niche edge cases that I didn't think of to include in the spec. Sometimes there will be cases where a human won't be able to think of all the edge cases. I think this is where AI can really shine. In the spec I could specify to test 50+ different edge cases and ways the app could break. This would also include telling it to test the app and try different ways to trigger undesired behavior. The AI could do all of this faster than humans would be able to test manually. I think including a testing portion in the spec would have been able to ensure the app was really bug free at the end. 

When would you use this workflow? Based on this experience, when do you think plan-delegate-review is better than conversational back-and-forth? When is it worse?

I think this workflow is good when you have time and when you are working in a very involved project with many moving parts. I think this was one of them where there were tons of different requirements to implement. Therefore, it would be useful laying out a detailed spec beforehand and telling the AI how to handle each case. If you had a lot of time I think you could try to use this workflow for a super big project by splitting that big project into small phases and use this workflow on each phase. However, I think if you are short on time and want to get a fast MVP the conversational back and forth would be ideal. It might also be worse if you wanted to iterate on the project with AI. For example, if you didn't have the entire spec thought out it might be helpful to use AI as a brainstorming tool. But the plan-delegate-review framework is better in terms of making everything bug proof and testing everything rigorously. Honestly, I believe that the two workflows could be combined effectively. Maybe if you write the spec using the help of conversational back and forth. Then having the AI agent implement the phase + review. And then iterating on the project with conversational back and forth. 